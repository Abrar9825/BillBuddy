<!DOCTYPE html>
<html lang="en">
<head>
  <title>Join Tracker</title>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont/tabler-icons.min.css">
  <style>
    body {
      background: #fff;
      background-image: radial-gradient(#1e293b 0.7px, transparent 0.7px);
      background-size: 18px 18px;
    }
    .card-3d {
      box-shadow: 0 8px 32px 0 #1e293b13, 0 2px 8px 0 #1e293b13;
      border-radius: 1.5rem;
      background: #fff;
      border: 1px solid #e5e7eb;
    }
    .input-3d {
      background: #f8fafc;
      border: 1px solid #e5e7eb;
      box-shadow: 0 2px 8px 0 #1e293b0a inset;
      transition: border 0.15s, background 0.15s;
    }
    .input-3d:focus {
      border-color: #1e293b;
      background: #f1f5f9;
      color: #1e293b;
    }
  </style>
</head>
<body class="min-h-screen flex flex-col items-center font-inter">
  <%- include('partials/navbar') %>
  <div class="flex-grow flex items-center justify-center w-full">
  <div class="w-full max-w-md p-1 card-3d">
    <form action="/join" method="POST" class="rounded-2xl p-8">
      <h2 class="text-3xl font-extrabold text-center text-[#1e293b] mb-7 select-none tracking-tight">
        Join Tracker
      </h2>
      <p class="text-[#64748b] text-center mb-8 text-base">
        Please enter your details to join the tracker. Your name and number will be visible to other group members for expense management.
      </p>
      <input type="hidden" name="trackerId" value="<%= trackerId %>">
      <div class="mb-6">
        <label for="name" class="block text-sm font-semibold text-[#1e293b] mb-2">Your Name</label>
        <input id="name" name="name" required placeholder="e.g. Suhasee"
          class="w-full px-3 py-2 rounded-xl input-3d focus:outline-none text-[#1e293b] placeholder-[#64748b] text-base" autocomplete="off" />
      </div>
      <div class="mb-6">
        <label for="number" class="block text-sm font-semibold text-[#1e293b] mb-2">Your Number</label>
        <input id="number" name="number" required placeholder="e.g. 9875000000"
          class="w-full px-3 py-2 rounded-xl input-3d focus:outline-none text-[#1e293b] placeholder-[#64748b] text-base" autocomplete="off" />
      </div>
      <button type="submit"
        class="w-full py-2 bg-[#1e293b] hover:bg-[#334155] text-white rounded-xl font-bold text-lg shadow-md transition">
        Join Tracker
      </button>
      <p class="text-center mt-6 text-sm text-[#64748b]">
        Want to start a new group? <a href="/" class="text-[#2563eb] underline hover:text-[#1e293b]">Create a new tracker</a>
      </p>
    </form>
  </div>
  </div>
</body>
</html>